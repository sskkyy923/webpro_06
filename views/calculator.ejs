<!DOCTYPE html>
<html lang="ja">
<head>
    <title>Calculator</title>
</head>
<body>
    <h1>電卓機能</h1>
    <form action="/calculator" method="get">
        <input type="number" name="num1" placeholder="数値1" value="<%= num1 %>" required>
        <select name="operator">
            <option value="+" <% if (operator === "+") { %>selected<% } %>>+</option>
            <option value="-" <% if (operator === "-") { %>selected<% } %>>-</option>
            <option value="*" <% if (operator === "*") { %>selected<% } %>>*</option>
            <option value="/" <% if (operator === "/") { %>selected<% } %>>/</option>
        </select>
        <input type="number" name="num2" placeholder="数値2" value="<%= num2 %>" required>
        <button type="submit">計算</button>
    </form>
    <h2>結果: <%= result %></h2>
</body>
</html>
